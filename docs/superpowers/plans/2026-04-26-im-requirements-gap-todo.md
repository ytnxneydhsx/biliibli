# 5-即时通信未完成事项待办

日期：2026-04-26

参考原始要求：

- [/Users/huangnv/code/biliibli/bilibili_SpringBoot/5-即时通信.md](/Users/huangnv/code/biliibli/bilibili_SpringBoot/5-即时通信.md)

## 目标

把当前项目和 `5-即时通信` 原始要求之间的缺口收敛成一份可执行待办，便于后续逐项补齐。

## 核心缺口

### 1. 补齐“按时间范围查询聊天记录”

- [ ] 单聊历史消息查询支持 `startTime` / `endTime`
- [ ] 群聊历史消息查询支持 `startTime` / `endTime`
- [ ] 保留现有 `beforeServerMessageId` 翻页能力，明确与时间筛选的组合规则
- [ ] 前端消息页增加“按时间范围筛选聊天记录”入口
- [ ] 为时间范围查询补充索引与 SQL 评估，避免直接扫全量消息表

## 验收标准

- 单聊和群聊都能查询指定时间范围内的聊天记录
- 时间筛选和历史翻页可以稳定配合使用

### 2. 补齐“屏蔽 / 拉黑”完整链路

- [ ] 为 `contact_relation.is_blocked` 增加写能力
- [ ] 提供拉黑、取消拉黑接口
- [ ] 为拉黑操作增加缓存失效，保持 `im:relation-snapshot` 一致
- [ ] 在前端会话或用户页增加拉黑 / 取消拉黑入口
- [ ] 明确拉黑后的行为：
  - [ ] 是否禁止对方继续发送消息
  - [ ] 是否隐藏历史会话
  - [ ] 是否影响会话列表展示

## 验收标准

- 用户可以主动拉黑和取消拉黑
- 被拉黑方无法继续给对方发送私信
- 关系缓存与实际权限判断保持一致

### 3. 补齐“群禁言后禁止发送”闭环

- [ ] 在群消息发送前接入 `isAllMuted` 判断
- [ ] 在群消息发送前接入成员 `isMuted` 判断
- [ ] 明确全员禁言下群主 / 管理员是否仍可发言
- [ ] 为这部分规则补测试，覆盖普通成员、管理员、群主三类角色

## 验收标准

- 成员被禁言后无法继续发群消息
- 群全员禁言后只有符合规则的角色可以发言
- 前端和后端错误提示一致

### 4. 联系人模块从“关注关系”里独立出来

- [ ] 明确 IM 联系人和关注 / 互关的边界
- [ ] 为联系人模块补独立的查询与展示能力
- [ ] 明确 `contact_relation.is_contact` 的维护来源
- [ ] 前端把 IM 联系人列表和普通关注 / 好友列表区分开

## 验收标准

- 联系人模块不再只是依赖关注系统页面间接表达
- IM 联系人、关注、好友三者语义清晰

### 5. 整理题目要求里的“思考题 / 设计题”正式答卷

- [ ] 增加一份统一的 IM 设计说明文档
- [ ] 明确回答以下问题：
  - [ ] 为什么选 WebSocket，而不是短轮询 / 长轮询
  - [ ] WebSocket 握手过程与身份验证方案
  - [ ] 端到端可靠性与一致性如何保证
  - [ ] TCP 已经可靠，为什么上层仍然要做可靠性设计
  - [ ] 幂等如何设计
  - [ ] 群聊时序性如何理解与处理
  - [ ] 读扩散 / 写扩散的选择、优缺点与当前实现
- [ ] 把现有零散文档统一挂到这份总文档里，避免答卷分散

## 验收标准

- 读文档即可回答题目后半段核心问题
- 代码实现和文档描述能一一对上

## Bonus 缺口

### 6. 聊天传输安全优化仍未落地

- [ ] HTTPS / WSS
- [ ] Cookie 化登录态
- [ ] 移除 WebSocket query token
- [ ] JWT secret / 生命周期收紧

说明：

- 这一部分已有单独待办文档：
  - [/Users/huangnv/code/biliibli/docs/superpowers/plans/2026-04-25-communication-security-first-tier.md](/Users/huangnv/code/biliibli/docs/superpowers/plans/2026-04-25-communication-security-first-tier.md)

### 7. Netty 版 WebSocket 接入未实现

- [ ] 评估是否需要作为 Bonus 单独落地
- [ ] 如果要做，优先先把现有 Spring WebSocket 接入层解耦干净

### 8. 压测 / Benchmark 结果还没整理成一份 IM 性能总结

- [ ] 整理现有监控与压测结果
- [ ] 输出一份 IM 性能说明文档
- [ ] 说明当前瓶颈、优化点和后续计划

## 建议实施顺序

1. 按时间范围查询聊天记录
2. 屏蔽 / 拉黑完整链路
3. 群禁言发送拦截闭环
4. 联系人模块独立化
5. 统一 IM 设计答卷文档
6. Bonus 项按精力补充
