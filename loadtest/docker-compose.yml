services:
  k6:
    image: grafana/k6:0.49.0
    working_dir: /work
    env_file:
      - .env
    entrypoint: ["k6"]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./scripts:/work/scripts:ro
      - ./data:/work/data:ro
      - ./results:/work/results
